# Publicationtoolcoreidcca

Core CCR intraday continuous clearing (window A). Endpoints carry the variant prefix `IDCCa_`; the `monitoring` system endpoint is shared with Core (see PublicationToolCoreDA tag).

## Allocation constraints

`GET /publicationtool/coreID/data/IDCCa_allocationConstraint`

```@raw html
<OAOperation operationId="publicationtool_idcc_a_allocation_constraint" prefix-headings="true" />
```

## Congestion income per border

`GET /publicationtool/coreID/data/IDCCa_congestionIncome`

```@raw html
<OAOperation operationId="publicationtool_idcc_a_congestion_income" prefix-headings="true" />
```

## D-2 congestion forecast

`GET /publicationtool/coreID/data/IDCCa_d2CF`

```@raw html
<OAOperation operationId="publicationtool_idcc_a_d2cf" prefix-headings="true" />
```

## Final FB domain

`GET /publicationtool/coreID/data/IDCCa_finalComputation`

```@raw html
<OAOperation operationId="publicationtool_idcc_a_final_computation" prefix-headings="true" />
```

## Initial FB domain

`GET /publicationtool/coreID/data/IDCCa_initialComputation`

```@raw html
<OAOperation operationId="publicationtool_idcc_a_initial_computation" prefix-headings="true" />
```

## Border intraday ATC

`GET /publicationtool/coreID/data/IDCCa_intradayAtc`

```@raw html
<OAOperation operationId="publicationtool_idcc_a_intraday_atc" prefix-headings="true" />
```

## Border intraday NTC

`GET /publicationtool/coreID/data/IDCCa_intradayNtc`

```@raw html
<OAOperation operationId="publicationtool_idcc_a_intraday_ntc" prefix-headings="true" />
```

## Max bilateral exchanges per border

`GET /publicationtool/coreID/data/IDCCa_maxExchanges`

```@raw html
<OAOperation operationId="publicationtool_idcc_a_max_exchanges" prefix-headings="true" />
```

## Min/max net positions per zone

`GET /publicationtool/coreID/data/IDCCa_maxNetPos`

```@raw html
<OAOperation operationId="publicationtool_idcc_a_max_net_positions" prefix-headings="true" />
```

## Net positions per zone

`GET /publicationtool/coreID/data/IDCCa_netPos`

```@raw html
<OAOperation operationId="publicationtool_idcc_a_net_position" prefix-headings="true" />
```

## Pre-final FB domain

`GET /publicationtool/coreID/data/IDCCa_preFinalComputation`

```@raw html
<OAOperation operationId="publicationtool_idcc_a_prefinal_computation" prefix-headings="true" />
```

## Intraday price spreads

`GET /publicationtool/coreID/data/IDCCa_priceSpread`

```@raw html
<OAOperation operationId="publicationtool_idcc_a_price_spread" prefix-headings="true" />
```

## Reference programs

`GET /publicationtool/coreID/data/IDCCa_refprog`

```@raw html
<OAOperation operationId="publicationtool_idcc_a_refprog" prefix-headings="true" />
```

## Scheduled exchanges per border

`GET /publicationtool/coreID/data/IDCCa_scheduledExchanges`

```@raw html
<OAOperation operationId="publicationtool_idcc_a_scheduled_exchanges" prefix-headings="true" />
```

## TSO validation reductions

`GET /publicationtool/coreID/data/IDCCa_validationReductions`

```@raw html
<OAOperation operationId="publicationtool_idcc_a_validation_reductions" prefix-headings="true" />
```
