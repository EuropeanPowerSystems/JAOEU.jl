# Owsmpauctions

Explicit cross-border capacity auctions (OWSMP). API key required (`AUTH_API_KEY` header).

## Auction results

`GET /owsmp/getauctions`

```@raw html
<OAOperation operationId="owsmp_get_auctions" prefix-headings="true" />
```

## Bids for a specific auction

`GET /owsmp/getbids`

```@raw html
<OAOperation operationId="owsmp_get_bids" prefix-headings="true" />
```

## List corridors

`GET /owsmp/getcorridors`

```@raw html
<OAOperation operationId="owsmp_get_corridors" prefix-headings="true" />
```

## Curtailment records for a corridor

`GET /owsmp/getcurtailment`

```@raw html
<OAOperation operationId="owsmp_get_curtailment" prefix-headings="true" />
```

## List auction horizons

`GET /owsmp/gethorizons`

```@raw html
<OAOperation operationId="owsmp_get_horizons" prefix-headings="true" />
```

