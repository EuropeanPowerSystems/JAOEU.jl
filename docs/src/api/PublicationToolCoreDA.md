# Publicationtoolcoreda

Core CCR day-ahead flow-based market coupling — domain, net positions, shadow prices, validations, monitoring.

## Allocation constraints (up/down per zone)

`GET /publicationtool/core/data/allocationConstraint`

```@raw html
<OAOperation operationId="publicationtool_core_da_allocation_constraint" prefix-headings="true" />
```

## Alpha factor per zone

`GET /publicationtool/core/data/alphaFactor`

```@raw html
<OAOperation operationId="publicationtool_core_da_alpha_factor" prefix-headings="true" />
```

## Congestion income per border

`GET /publicationtool/core/data/congestionIncome`

```@raw html
<OAOperation operationId="publicationtool_core_da_congestion_income" prefix-headings="true" />
```

## D-2 congestion forecast

`GET /publicationtool/core/data/d2CF`

```@raw html
<OAOperation operationId="publicationtool_core_da_d2cf" prefix-headings="true" />
```

## Final flow-based domain (CNECs + PTDFs)

`GET /publicationtool/core/data/finalComputation`

```@raw html
<OAOperation operationId="publicationtool_core_da_final_computation" prefix-headings="true" />
```

## Initial flow-based domain

`GET /publicationtool/core/data/initialComputation`

```@raw html
<OAOperation operationId="publicationtool_core_da_initial_computation" prefix-headings="true" />
```

## Long-term allocations

`GET /publicationtool/core/data/lta`

```@raw html
<OAOperation operationId="publicationtool_core_da_lta" prefix-headings="true" />
```

## Maximum bilateral exchanges per border

`GET /publicationtool/core/data/maxExchanges`

```@raw html
<OAOperation operationId="publicationtool_core_da_max_exchanges" prefix-headings="true" />
```

## Min/max net positions per zone

`GET /publicationtool/core/data/maxNetPos`

```@raw html
<OAOperation operationId="publicationtool_core_da_max_net_positions" prefix-headings="true" />
```

## Net positions per bidding zone

`GET /publicationtool/core/data/netPos`

```@raw html
<OAOperation operationId="publicationtool_core_da_net_position" prefix-headings="true" />
```

## Pre-final flow-based domain

`GET /publicationtool/core/data/preFinalComputation`

```@raw html
<OAOperation operationId="publicationtool_core_da_prefinal_computation" prefix-headings="true" />
```

## Day-ahead price spreads per border

`GET /publicationtool/core/data/priceSpread`

```@raw html
<OAOperation operationId="publicationtool_core_da_price_spread" prefix-headings="true" />
```

## Reference programs

`GET /publicationtool/core/data/refprog`

```@raw html
<OAOperation operationId="publicationtool_core_da_refprog" prefix-headings="true" />
```

## Scheduled exchanges per border

`GET /publicationtool/core/data/scheduledExchanges`

```@raw html
<OAOperation operationId="publicationtool_core_da_scheduled_exchanges" prefix-headings="true" />
```

## Active constraints with shadow prices

`GET /publicationtool/core/data/shadowPrices`

```@raw html
<OAOperation operationId="publicationtool_core_da_shadow_prices" prefix-headings="true" />
```

## Default-FBP status flag

`GET /publicationtool/core/data/spanningDefaultFBP`

```@raw html
<OAOperation operationId="publicationtool_core_da_spanning_default_fbp" prefix-headings="true" />
```

## TSO validation reductions

`GET /publicationtool/core/data/validationReductions`

```@raw html
<OAOperation operationId="publicationtool_core_da_validation_reductions" prefix-headings="true" />
```

## Process monitoring (deadlines, publication times)

`GET /publicationtool/core/system/monitoring`

```@raw html
<OAOperation operationId="publicationtool_core_da_monitoring" prefix-headings="true" />
```

